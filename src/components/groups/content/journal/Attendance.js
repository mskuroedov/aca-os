import React from 'react';
import { withStyles } from '@material-ui/core';

class Attendance extends React.Component {
  render() {
    return <div />;
  }
}

export default withStyles(theme => ({}))(Attendance);
